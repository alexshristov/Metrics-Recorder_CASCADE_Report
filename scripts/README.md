# Experiment Scripts

Place the scripts needed to reproduce the evaluation here.

The report should reference the final scripts from `sections/07_evaluation.tex` and `sections/06_implementation.tex`.
